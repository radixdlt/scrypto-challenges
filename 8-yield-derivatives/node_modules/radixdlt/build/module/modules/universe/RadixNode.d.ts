import { RadixNodeSystem } from '../..';
import Long from 'long';
export default class RadixNode {
    readonly nodeInfo: RadixNodeSystem;
    readonly wsAddress: string;
    readonly httpAddress: string;
    static SHARD_CHUNK_HALF_RANGE: Long;
    constructor(nodeInfo: RadixNodeSystem, wsAddress: string, httpAddress: string);
    canServiceShard(shard: Long): boolean;
}

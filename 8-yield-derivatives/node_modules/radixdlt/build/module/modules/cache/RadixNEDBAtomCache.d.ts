import { RadixAtomCacheProvider } from '../..';
import { RadixAtom, RadixAddress } from '../atommodel';
export default class RadixNEDBAtomCache implements RadixAtomCacheProvider {
    private db;
    /**
     * Creates an instance of radix nedbatom cache.
     * @param filename File path on disk in Node, path in localStorage in the browser
     */
    constructor(filename: string);
    /**
     * Clears all atoms from the cache
     */
    reset(): void;
    storeAtom: (atom: RadixAtom) => Promise<void | RadixAtom>;
    getAtoms: (queryAddress?: RadixAddress) => Promise<RadixAtom[]>;
    private asyncDeserialize;
    deleteAtom(atom: RadixAtom): Promise<number>;
    findOne: (opt: any) => Promise<RadixAtom>;
    notExists: (opt: any) => Promise<boolean>;
    find: (opt: any) => Promise<RadixAtom[]>;
    insert: (opt: any) => Promise<RadixAtom>;
    remove: (opt: any) => Promise<number>;
}

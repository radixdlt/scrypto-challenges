import { RadixAddress, RadixAtom } from '../atommodel';
export default interface RadixAtomCacheProvider {
    storeAtom(atom: RadixAtom): any;
    deleteAtom(atom: RadixAtom): any;
    getAtoms(keyPair?: RadixAddress): Promise<RadixAtom[]>;
    reset(): any;
}

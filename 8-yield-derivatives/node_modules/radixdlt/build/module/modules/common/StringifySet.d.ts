export declare class StringifySet<T extends {
    toString(): string;
}> {
    private map;
    constructor();
    static of<T>(items: T[]): StringifySet<T>;
    add(item: T): void;
    addAll(items: T[]): void;
    values(): T[];
    remove(item: T): void;
}

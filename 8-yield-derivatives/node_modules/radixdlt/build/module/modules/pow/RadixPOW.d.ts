/// <reference types="node" />
import Long from 'long';
export default class RadixPOW {
    readonly magic: number;
    readonly seed: Buffer;
    nonce: Long;
    constructor(magic: number, seed: Buffer);
    getHash(): Buffer;
    incrementNonce(): void;
}

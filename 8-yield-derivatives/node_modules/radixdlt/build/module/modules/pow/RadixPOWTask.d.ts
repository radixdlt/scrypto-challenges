/// <reference types="node" />
import RadixPOW from './RadixPOW';
export default class RadixPOWTask {
    readonly magic: number;
    readonly seed: Buffer;
    readonly target: Buffer;
    pow: RadixPOW;
    constructor(magic: number, seed: Buffer, target: Buffer);
    computePow(): Promise<RadixPOW>;
    attemptPow(callback: (pow: RadixPOW) => void): void;
    meetsTarget(hash: Buffer): boolean;
}

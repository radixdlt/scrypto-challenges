/// <reference types="node" />
import { RadixPrimitive, RadixAddress } from '..';
export declare class RRI implements RadixPrimitive {
    readonly address: RadixAddress;
    readonly name: string;
    constructor(address: RadixAddress, name: string);
    static fromJSON(uri: string): RRI;
    static fromString(uri: string): RRI;
    toJSON(): string;
    toString(): string;
    toDSON(): Buffer;
    encodeCBOR(encoder: any): any;
    equals(rri: RRI): boolean;
    getAddress(): RadixAddress;
    getName(): string;
}

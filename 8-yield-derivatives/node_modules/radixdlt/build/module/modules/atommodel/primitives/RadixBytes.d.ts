/// <reference types="node" />
import { RadixPrimitive } from '..';
export declare class RadixBytes implements RadixPrimitive {
    readonly bytes: Buffer;
    constructor(bytes: any);
    static fromEncoded(encoded: string): RadixBytes;
    static fromJSON(bytes: string): RadixBytes;
    toJSON(): string;
    toDSON(): Buffer;
    encodeCBOR(encoder: any): any;
    toString(): string;
    asJSON(): any;
}

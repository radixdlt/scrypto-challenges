/// <reference types="node" />
import { RadixPrimitive } from '..';
import long from 'long';
export declare class RadixEUID implements RadixPrimitive {
    readonly bytes: Buffer;
    readonly shard: long;
    constructor(value: number | Buffer | number[] | string);
    static fromJSON(data: string): RadixEUID;
    toJSON(): string;
    toDSON(): Buffer;
    encodeCBOR(encoder: any): any;
    equals(euid: RadixEUID): boolean;
    toString(): string;
    getShard(): long;
}

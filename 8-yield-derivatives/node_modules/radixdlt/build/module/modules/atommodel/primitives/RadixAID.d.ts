/// <reference types="node" />
import long from 'long';
import { RadixPrimitive } from '..';
export declare class RadixAID implements RadixPrimitive {
    static BYTES: number;
    static HASH_BYTES: number;
    static SHARD_BYTES: number;
    private bytes;
    constructor(bytes: Buffer);
    static from(hash: Buffer, shards: long[]): RadixAID;
    static fromJSON(data: string): RadixAID;
    toJSON(): string;
    toDSON(): Buffer;
    encodeCBOR(encoder: any): any;
    equals(aid: RadixAID): boolean;
    toString(): string;
    getShard(): long;
}

/// <reference types="node" />
import { RadixPrimitive } from '..';
export declare class RadixHash implements RadixPrimitive {
    readonly bytes: Buffer;
    constructor(bytes: any);
    static fromJSON(data: string): RadixHash;
    toJSON(): string;
    toDSON(): Buffer;
    encodeCBOR(encoder: any): any;
}

import { RadixParticle, RadixAddress, RadixBytes } from '../..';
/**
 * Particle which can hold arbitrary data
 */
export declare class RadixMessageParticle extends RadixParticle {
    from: RadixAddress;
    to: RadixAddress;
    metaData: {
        [s: string]: string;
    };
    bytes: RadixBytes;
    nonce: number;
    constructor(from: RadixAddress, to: RadixAddress, data: any, metaData: {
        [s: string]: string;
    }, nonce?: number);
    getAddresses(): RadixAddress[];
    getData(): RadixBytes;
    getMetaData(key: string): string;
    getNonce(): number;
}

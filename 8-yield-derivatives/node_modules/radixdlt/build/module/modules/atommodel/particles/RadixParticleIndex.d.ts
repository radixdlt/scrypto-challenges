import { RadixSerializableObject, RadixAddress } from '..';
export declare class RadixParticleIndex extends RadixSerializableObject {
    address: RadixAddress;
    unique: string;
    constructor(address: RadixAddress, unique: string);
}

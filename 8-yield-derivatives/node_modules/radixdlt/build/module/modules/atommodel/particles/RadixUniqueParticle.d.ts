import { RadixParticle, RadixAddress, RRI } from '..';
export declare class RadixUniqueParticle extends RadixParticle {
    name: string;
    address: RadixAddress;
    nonce: number;
    constructor(address: RadixAddress, unique: string, nonce?: number);
    getAddresses(): RadixAddress[];
    getRRI(): RRI;
}

import { RadixParticle, RadixAddress, RadixFungibleType, RadixUInt256, RRI, RadixOwnable, RadixFungible, RadixConsumable, RadixTokenPermissions } from '../..';
import BN from 'bn.js';
/**
 *  A particle which represents an amount of consumable and consuming, tranferable fungible tokens
 *  owned by some key owner and stored in an account.
 */
export declare class RadixTransferrableTokensParticle extends RadixParticle implements RadixOwnable, RadixFungible, RadixConsumable {
    address: RadixAddress;
    tokenDefinitionReference: RRI;
    granularity: RadixUInt256;
    planck: number;
    nonce: number;
    amount: RadixUInt256;
    permissions: RadixTokenPermissions;
    constructor(amount: BN, granularity: BN, address: RadixAddress, nonce: number, tokenReference: RRI, tokenPermissions: RadixTokenPermissions, planck?: number);
    getAddress(): RadixAddress;
    getAddresses(): RadixAddress[];
    getType(): RadixFungibleType;
    getPlanck(): number;
    getNonce(): number;
    getTokenDefinitionReference(): RRI;
    getOwner(): RadixAddress;
    getAmount(): BN;
    getGranularity(): BN;
    getTokenPermissions(): RadixTokenPermissions;
}

import { RadixAddress } from '../atommodel';
interface KeystoreData {
    crypto: {
        cipher: string;
        cipherparams: {
            iv: string;
        };
        ciphertext: string;
        pbkdfparams: {
            iterations: number;
            keylen: number;
            digest: string;
            salt: string;
        };
        mac: string;
    };
    id: string;
}
export default class RadixKeyStore {
    /**
     * Encrypt a private key
     * @param address
     * @param password
     * @returns
     */
    static encryptKey(address: RadixAddress, password: string): Promise<KeystoreData>;
    /**
     * Decrypts an encrypted private key
     * @param fileContents
     * @param password
     * @returns key
     */
    static decryptKey(fileContents: KeystoreData, password: string): Promise<RadixAddress>;
    private static calculateMac;
}
export {};

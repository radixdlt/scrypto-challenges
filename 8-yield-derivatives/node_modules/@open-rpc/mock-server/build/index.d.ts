import { OpenrpcDocument as OpenRPC } from "@open-rpc/meta-schema";
import { Server } from "@open-rpc/server-js";
export declare const serviceMode: (port: number, openrpcDocument: OpenRPC) => Server;
declare const server: (port: number, openrpcDocument: OpenRPC) => Server;
export default server;

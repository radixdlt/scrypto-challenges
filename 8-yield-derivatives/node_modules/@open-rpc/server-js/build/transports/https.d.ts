/// <reference types="node" />
import cors from "cors";
import { HandleFunction } from "connect";
import { SecureServerOptions } from "http2";
import ServerTransport from "./server-transport";
export interface HTTPSServerTransportOptions extends SecureServerOptions {
    middleware: HandleFunction[];
    port: number;
    cors?: cors.CorsOptions;
    allowHTTP1?: boolean;
}
export default class HTTPSServerTransport extends ServerTransport {
    private options;
    private static defaultCorsOptions;
    private server;
    constructor(options: HTTPSServerTransportOptions);
    start(): void;
    stop(): void;
    private httpsRouterHandler;
}

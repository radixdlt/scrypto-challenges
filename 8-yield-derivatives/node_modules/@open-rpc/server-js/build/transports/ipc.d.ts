import ServerTransport from "./server-transport";
export interface IPCServerTransportOptions {
    id: string;
    port: number;
    udp: boolean;
    ipv6: boolean;
}
export default class IPCServerTransport extends ServerTransport {
    private options;
    private server;
    constructor(options: IPCServerTransportOptions);
    start(): void;
    stop(): void;
    private ipcRouterHandler;
}

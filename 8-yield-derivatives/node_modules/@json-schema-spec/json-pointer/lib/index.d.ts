export default class Ptr {
    static parse(s: string): Ptr;
    tokens: string[];
    constructor(tokens: string[]);
    toString(): string;
    eval(instance: any): any;
}
export declare class InvalidPtrError extends Error {
    ptr: string;
    constructor(ptr: string);
}
export declare class EvalError extends Error {
    instance: any;
    token: string;
    constructor(instance: any, token: string);
}

import { ResultAsync } from 'neverthrow';
import { AppLogger } from '../helpers/logger';
import { SdkError } from '../wallet-sdk';
import { Metadata, WalletInteraction, WalletInteractionItems } from './schemas';
export declare const walletInteractionFactory: (logger?: AppLogger) => (metadata: Metadata, items: WalletInteractionItems, interactionId?: `${string}-${string}-${string}-${string}-${string}`) => ResultAsync<WalletInteraction, SdkError>;

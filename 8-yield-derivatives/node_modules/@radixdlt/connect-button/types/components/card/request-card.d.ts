import { LitElement } from 'lit';
import './card';
import '../account/account';
import '../link/link';
import { RadixButtonMode, RequestItemType, RequestStatus } from '../../_types';
export declare class RadixRequestCard extends LitElement {
    mode: RadixButtonMode;
    type: keyof typeof RequestItemType;
    status: keyof typeof RequestStatus;
    showCancel: boolean;
    timestamp: string;
    id: string;
    transactionIntentHash: string;
    render(): import("lit").TemplateResult<1>;
    private getRequestContentTemplate;
    private getIconFromStatus;
    private getStylingFromStatus;
    private onCancel;
    private renderTxIntentHash;
    static styles: import("lit").CSSResult[];
}
declare global {
    interface HTMLElementTagNameMap {
        'radix-request-card': RadixRequestCard;
    }
}

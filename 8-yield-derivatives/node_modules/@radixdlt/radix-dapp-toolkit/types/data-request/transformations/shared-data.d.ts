import { WalletSdk } from '@radixdlt/wallet-sdk';
import { SharedData } from '../../state/types';
import { DataRequestState } from '../builders';
export declare const transformWalletRequestToSharedData: (walletDataRequest: Parameters<WalletSdk['request']>[0], sharedData: SharedData) => SharedData;
export declare const transformSharedDataToDataRequestState: (sharedData: SharedData) => DataRequestState;

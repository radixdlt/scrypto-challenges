import { WalletData } from '../../state/types';
export declare const transformWalletDataToConnectButton: (walletData: WalletData) => {
    accounts: {
        address: string;
        label: string;
        appearanceId: number;
    }[];
    personaLabel: string;
    connected: boolean;
    personaData: {
        value: string;
        field: string;
    }[];
};

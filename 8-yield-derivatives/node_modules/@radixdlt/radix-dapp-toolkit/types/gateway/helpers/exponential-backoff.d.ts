import { SdkError } from '@radixdlt/wallet-sdk';
import { Result } from 'neverthrow';
import { Observable, Subject } from 'rxjs';
export type ExponentialBackoffInput = {
    multiplier?: number;
    maxDelayTime?: number;
    timeout?: number;
    interval?: number;
};
export type ExponentialBackoff = typeof ExponentialBackoff;
export declare const ExponentialBackoff: ({ maxDelayTime, multiplier, timeout, interval, }?: ExponentialBackoffInput) => {
    trigger: Subject<void>;
    withBackoff$: Observable<Result<number, SdkError>>;
};

# Reads the NFT address of the Participants instance a fund uses.
#
# Parameters:
#
# component: address of the Radfolio instance to use
CALL_METHOD
   ComponentAddress("${component}")
   "read_participants_nft_address";

# Reads the amount of protocol fees currently stored in a fund.
#
# Parameters:
#
# component: address of the Radfolio instance to use
CALL_METHOD
   ComponentAddress("${component}")
   "read_fees_stored";

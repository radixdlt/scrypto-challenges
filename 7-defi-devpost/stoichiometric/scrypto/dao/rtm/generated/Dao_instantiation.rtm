CALL_METHOD
	ComponentAddress("${caller_address}")
	"lock_fee"
	Decimal("100");

CALL_FUNCTION
	PackageAddress("${package_address}")
	"Dao"
	"new"
	${arg_0}i64
	Decimal("${arg_1}")
	ResourceAddress("${arg_2}")
	Decimal("${arg_3}")
	Decimal("${arg_4}")
	Decimal("${arg_5}")
	Decimal("${arg_6}")
	ComponentAddress("${arg_7}")
	Decimal("${arg_8}")
	Decimal("${arg_9}")
	Decimal("${arg_10}");

CALL_METHOD
	ComponentAddress("${caller_address}")
	"deposit_batch"
	Expression("ENTIRE_WORKTOP");